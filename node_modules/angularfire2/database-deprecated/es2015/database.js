import { Inject, Injectable, Optional, NgZone } from '@angular/core';
import { FirebaseListFactory } from './firebase_list_factory';
import { FirebaseObjectFactory } from './firebase_object_factory';
import * as utils from './utils';
import { FirebaseAppConfigToken, FirebaseOptionsToken, FirebaseAppNameToken, RealtimeDatabaseURL, _firebaseAppFactory } from 'angularfire2';
export class AngularFireDatabase {
    constructor(options, config, name, databaseURL, zone) {
        this.database = zone.runOutsideAngular(() => {
            const app = _firebaseAppFactory(options, name, config);
            return app.database(databaseURL || undefined);
        });
    }
    list(pathOrRef, opts) {
        const ref = utils.getRef(this.database, pathOrRef);
        return FirebaseListFactory(ref, opts);
    }
    object(pathOrRef, opts) {
        const ref = utils.getRef(this.database, pathOrRef);
        return FirebaseObjectFactory(ref, opts);
    }
}
AngularFireDatabase.decorators = [
    { type: Injectable },
];
AngularFireDatabase.ctorParameters = () => [
    { type: undefined, decorators: [{ type: Inject, args: [FirebaseOptionsToken,] },] },
    { type: undefined, decorators: [{ type: Inject, args: [FirebaseAppConfigToken,] },] },
    { type: undefined, decorators: [{ type: Optional }, { type: Inject, args: [FirebaseAppNameToken,] },] },
    { type: undefined, decorators: [{ type: Optional }, { type: Inject, args: [RealtimeDatabaseURL,] },] },
    { type: NgZone, },
];
export { RealtimeDatabaseURL };
//# sourceMappingURL=database.js.map